import WikiEngine from '.';

export default class WikiDocumentEngine {
  // Engines
  wikiEngine: WikiEngine;

  constructor(wikiEngine: WikiEngine) {
    this.wikiEngine = wikiEngine;
  }

  // [] CRUD
  // [] Check authorization
  // [] Checking conflicts
  // [] Structure
  // [] TextContent
  // [] Reward calculation
}
